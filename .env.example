# Backend Environment Variables
PORT=3001
NODE_ENV=production

# MongoDB Connection
# Choose ONE of the following based on your setup:

# Option 1: Docker MongoDB (when using docker-compose.yml)
MONGODB_URI=mongodb://mongodb:27017/recipeStore

# Option 2: Host machine MongoDB (when using docker-compose.host-mongo.yml)
# MONGODB_URI=mongodb://host.docker.internal:27017/recipeStore

# Option 3: External MongoDB server (separate host)
# MONGODB_URI=mongodb://10.0.0.85:27017/recipeStore
# MONGODB_URI=mongodb://mongo.example.com:27017/recipeStore

# Option 4: External MongoDB with authentication
# MONGODB_URI=mongodb://username:password@mongo.example.com:27017/recipeStore?authSource=admin

# Option 5: MongoDB Atlas (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/recipeStore?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=your-secret-key-here-change-this-to-something-secure
JWT_EXPIRATION=24h

# CORS Configuration
# Add your frontend URLs (comma-separated)
ALLOWED_ORIGINS=http://localhost,http://localhost:80,http://your-domain.com
